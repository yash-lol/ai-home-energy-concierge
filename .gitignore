# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Runtime artifacts produced by the hub
code/hub/.last_report.json
*.log

# Local demo control file (documented in the README; not shared state)
loads.json

# OS / editor
Thumbs.db
Desktop.ini
.DS_Store
.vscode/
.idea/
*.swp

# Board-side runtime config (LAN IPs, machine-specific). Copy from board.env.example.
code/arduino/board.env
# Local run artifacts
code/*.log
code/hub_server*.log

# Demo recording lives next to presentation.html but is not committed by default
# (large binary). Remove this line if you want it in the repo for submission.
demo.mp4

# local run logs
code/*.err
code/arduino/pc.env

# Recorded sessions: household sensor data, stays on the machine that made it.
# The project claims occupancy data never leaves the house — that has to remain
# true of the repo too. Force-add a curated sample with `git add -f` if you want
# one committed as evidence.
code/data/sessions/
# Downloaded third-party corpora — re-fetch with tools/train_occupancy.py
code/data/uci/
